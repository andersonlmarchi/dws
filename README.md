# Docker Workspace Switcher (DWS)

A CLI tool that helps developers switch between Docker-based development workspaces with a single command.

Stop one project.
Start another.
Keep your machine clean and focused.

Use `npm link` to associate this repository, `dws` command, to your bash.
